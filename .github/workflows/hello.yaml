
name: Hello Github Actions

on:
    push:
      branches: [develop]

jobs:
  say_hello:
   runs-on: ubuntu-latest
     steps:
       - name: print greeting
         run: echo "hello, from Github Actions!"